rules:

- prefix of xc. to avoid clashes elsewhere
- suffix is instruction varient

  - i = immediate version
  - u = updating (vs. overwriting) version
  - m = multiple
  - cl = carryless version
  - b/h/w = byte/halfword/word varient
  - $ is a width

XCR2GPR    => xc.xcr2gpr
MV2COP    => xc.gpr2xcr

LUI.cr    => xc.ld.hi
LLI.cr    => xc.ld.hi
LBU.cr    => xc.ld.bu
LHU.cr    => xc.ld.hu
          => xc.ld.b
          => xc.ld.h
LW.cr     => xc.ld.w
SB.cr     => xc.st.b
SH.cr     => xc.st.h
SW.cr     => xc.st.w

RSEED.cr  => xc.rngseed
RSAMP.cr  => xc.rngsamp
          => xc.rngtest

SCATTER.b => xc.scatter.b
SCATTER.h => xc.scatter.h
GATHER.b  => xc.gather.b
GATHER.h  => xc.gather.h

AND.cr    => xc.and
OR.cr     => xc.ior
XOR.cr    => xc.xor
BOP.cr    => xc.bop
EXT.cr    => xc.ext
INS.cr    => xc.ins
LMIX.cr   => xc.mix.l
HMIX.cr   => xc.mix.h

ADD.px    => xc.padd.$
SUB.px    => xc.psub.$
MUL.px    => xc.pmul.l.$
          => xc.pmul.h.$
          => xc.pclmul.l.$
          => xc.pclmul.h.$
SLL.px    => xc.psll.$
SLLI.px   => xc.psll.$i
SRL.px    => xc.psrl.$
SRLI.px   => xc.psrl.$i
ROT.px    => xc.prot.$
ROTI.px   => xc.prot.$i

TWID.B    => xc.pperm.b
TWID.NN   => xc.pperm.h$
TWID.CN   => xc.pperm.w$

ADD2.mp   => xc.madd.2
ADD3.mp   => xc.madd.3
SUB2.mp   => xc.msub.2
SUB3.mp   => xc.msub.3
ACC1.mp   => xc.macc.1
ACC2.mp   => xc.macc.2
MAC.mp    => xc.mmul.1
MAA.mp    => xc.mmul.2
          => xc.mclmul.1
          => xc.mclmul.2
SLLR.mp   => xc.msll
SLLI.mp   => xc.msll.i
SRLR.mp   => xc.msrl
SRLI.mp   => xc.msrl.i
EQU.mp    => xc.mequ
LTU.mp    => xc.mlte
GTU.mp    => xc.mgte

